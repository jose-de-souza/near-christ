<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the requested file doesn't exist...
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # ...then rewrite everything to index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>
