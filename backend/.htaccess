<IfModule mod_rewrite.c>
  RewriteEngine On

  # If the request isn't already for something in /public,
  # rewrite it to /public/{whatever was requested}.
  RewriteCond %{REQUEST_URI} !^/public/
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
